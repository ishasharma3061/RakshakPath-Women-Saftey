{
  "name": "rakshakpath",
  "version": "1.0.0",
  "description": "RakshakPath - Women Safety App",
  "scripts": {
    "start": "concurrently --kill-others-on-fail --prefix-colors \"bgBlue.bold,bgGreen.bold\" --prefix \"[{name}]\" --names \"BACKEND,FRONTEND\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
